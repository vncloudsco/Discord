namespace Squirrel.Update
{
    using System;

    internal enum StandardHandles
    {
        STD_INPUT_HANDLE = -10,
        STD_OUTPUT_HANDLE = -11,
        STD_ERROR_HANDLE = -12
    }
}

